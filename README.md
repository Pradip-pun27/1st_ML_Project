app.py,index.html and style.css are for deploying.
whereas FinalCode_of_Project.py, housing.csv, input.csv and main.py are for Training and doing Inference from model.
model.pkl and pipeline.pkl files aren't a/v which are used for deploying the trained model thr Flask but good news is FinalCode_of_Project.py file will generate those required .pkl files
